
# LA FUNCION DIR()
# La función devuelve una lista ordenada alfabéticamente que contiene todos las entidades disponibles en el módulo.
# Nota: Si el nombre del módulo tiene un alias, debes usar el alias, no el nombre original.
# el módulo debe haberse importado previamente como un todo (es decir, utilizar la instrucción import module)

import math
dir(math) # funcion que lista las entidades de un modulo en especifico
print(dir(math))